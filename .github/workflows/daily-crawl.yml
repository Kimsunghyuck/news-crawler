name: Daily Multi-Category News Crawl

on:
  schedule:
    # 매일 한국시간 오전 9시 (UTC 0시)
    - cron: '0 0 * * *'
    # 매일 한국시간 오후 3시 (UTC 6시)
    - cron: '0 6 * * *'
    # 매일 한국시간 저녁 7시 (UTC 10시)
    - cron: '0 10 * * *'
  workflow_dispatch:  # 수동 실행 버튼 활성화

jobs:
  crawl:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 코드 체크아웃
      uses: actions/checkout@v4
      
    - name: 🐍 Python 설정
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'
        
    - name: 📦 의존성 설치
      run: |
        pip install -r requirements.txt
        
    - name: 🕷️ 멀티 카테고리 뉴스 크롤링 실행
      run: |
        python crawler.py
        
    - name: 📊 트렌드 분석 실행
      run: |
        echo "📈 트렌드 키워드 분석 중..."
        python analyzer.py
        echo "✅ 트렌드 분석 완료"

    - name: 🧹 오래된 데이터 정리 (30일 이상)
      run: |
        echo "🧹 30일 이상 지난 데이터 정리 중..."
        python cleanup_old_data.py
        echo "✅ 데이터 정리 완료"

    - name: 🗑️ 기존 타임스탬프 없는 파일 삭제
      run: |
        echo "🗑️ 타임스탬프 없는 기존 파일 삭제 중..."
        find docs/data -type f -name "news_*[0-9].json" ! -name "*_*-*.json" -delete || true
        echo "✅ 기존 파일 삭제 완료"

    - name: 📋 GitHub Pages용 데이터 복사
      run: |
        echo "📂 docs 폴더로 데이터 복사 중..."
        cp -r data/* docs/data/
        echo "✅ 데이터 복사 완료"
        
    - name: 📊 크롤링 결과 확인
      run: |
        echo "📁 생성된 파일 목록 (타임스탬프 포함):"
        echo "=== 오늘 생성된 타임스탬프 파일 ==="
        TODAY=$(TZ='Asia/Seoul' date +'%Y-%m-%d')
        find data -type f -name "news_${TODAY}_*.json" | head -10
        echo ""
        echo "=== 카테고리별 파일 개수 ==="
        for category in politics sports economy society international culture; do
          count=$(find data/$category -type f -name "news_*_*.json" 2>/dev/null | wc -l)
          echo "$category: $count 개"
        done
        echo "=== 트렌드 데이터 ==="
        ls -la docs/data/trends/ 2>/dev/null || echo "트렌드: 없음"
        echo "=== 보고서 ==="
        ls -la reports/combined/ 2>/dev/null || echo "보고서: 없음"
        
    - name: 💾 결과 파일 커밋 및 푸시
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "GitHub Actions Bot"
        git add data/ reports/ logs/ docs/data/

        if ! git diff --quiet --staged; then
          git commit -m "🤖 자동 크롤링 (6개 카테고리 + 트렌드): $(TZ='Asia/Seoul' date +'%Y-%m-%d %H:%M:%S KST')"

          # Push with retry logic (handles concurrent workflow runs)
          MAX_RETRIES=3
          RETRY_COUNT=0
          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            if git push; then
              echo "✅ 변경사항이 커밋되고 푸시되었습니다."
              break
            else
              RETRY_COUNT=$((RETRY_COUNT + 1))
              echo "⚠️ Push 실패. Rebase 후 재시도 중... ($RETRY_COUNT/$MAX_RETRIES)"
              git pull --rebase origin main
              if [ $RETRY_COUNT -eq $MAX_RETRIES ]; then
                echo "❌ $MAX_RETRIES 번 시도 후 실패했습니다."
                exit 1
              fi
              sleep 5
            fi
          done
        else
          echo "ℹ️ 새로운 변경사항이 없습니다."
        fi
